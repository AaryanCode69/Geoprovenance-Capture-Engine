"""The visual demonstration — turning the recorded work into a QGIS project.

Owner: Person A.  **This is not plugin code.** RULES.md §1.1 lists exactly what
the plugin package is, and drawing maps is not on that list, so this lives
outside ``geoprovenance/`` and nothing under ``geoprovenance/`` imports it.

Start at ``README.md`` in this directory, or run ``make qgis-demo``.
"""
