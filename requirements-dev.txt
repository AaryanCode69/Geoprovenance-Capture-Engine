# Development and test dependencies only.
#
# RULES.md §2.2 — plugin code under geoprovenance/ uses the Python standard
# library plus PyQGIS/PyQt5 and NOTHING else. Nothing in this file may be
# imported from geoprovenance/.
#
# RULES.md §2.1 — install into a venv built on the SAME Python that ships with
# your QGIS 3.34 LTS. Check with, in the QGIS Python console:
#     import sys; sys.version

pytest>=7.0
pytest-qgis>=2.0
jsonschema>=4.0
