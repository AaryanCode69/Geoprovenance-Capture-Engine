"""Plugin UI. Person A owns only the shell; Person C owns what goes inside it.

RULES.md §1.2 — DAG layout, QGraphicsScene rendering, node/edge status colours
and the audit report are Person C's. This package contains the dock widget
placeholder and nothing else.
"""
